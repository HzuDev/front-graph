---
import Layout from '../layouts/Layout.astro';
import EncuestasPage from '../components/encuestas/EncuestasPage';
---

<Layout title="Monitor Electoral Bolivia | Encuestas">
  <div
    class="min-h-screen pt-32 pb-24 px-6 relative overflow-hidden bg-primary-green"
  >
    <div
      class="max-w-7xl mx-auto relative z-10 transition-all duration-1000 slide-up pt-12"
    >
      <h1
        class="text-5xl md:text-8xl font-black text-hunter tracking-tighter leading-[0.85] text-balance mb-12"
      >
        Encuestas y Sondeos
      </h1>
      <p class="text-xl text-hunter/60 md:w-2/3 mb-16">
        Explora las encuestas de intención de voto y otras mediciones de opinión
        pública relacionadas con las Elecciones 2026.
      </p>
      <EncuestasPage client:only="react" />
    </div>
  </div>
</Layout>

<style>
  .slide-up {
    animation: slideUp 1s ease-out forwards;
    opacity: 0;
  }

  @keyframes slideUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>
