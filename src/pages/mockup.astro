---
import Layout from "../layouts/Layout.astro";
import { ArrowRight, Fingerprint, ShieldCheck, Zap, Globe } from "lucide-react";
---

<Layout title="Bienvenido | Monitor Electoral">
    <main
        class="min-h-screen bg-[#0d1611] flex items-center justify-center p-4 md:p-8 font-sans antialiased overflow-hidden"
    >
        <div class="fixed inset-0 overflow-hidden pointer-events-none">
            <div
                class="absolute -top-[10%] -left-[10%] w-[40%] h-[40%] bg-[#14281d] rounded-full blur-[120px] opacity-50"
            >
            </div>
            <div
                class="absolute bottom-[10%] right-[10%] w-[30%] h-[30%] bg-[#fffcdc]/5 rounded-full blur-[100px]"
            >
            </div>
        </div>

        <div
            class="relative w-full max-w-6xl min-h-[85vh] grid grid-cols-1 lg:grid-cols-12 bg-[#1a2c21]/40 backdrop-blur-2xl rounded-[2.5rem] border border-white/10 shadow-2xl overflow-hidden"
        >
            <div
                class="lg:col-span-7 relative p-8 md:p-16 flex flex-col justify-between overflow-hidden border-b lg:border-b-0 lg:border-r border-white/5"
            >
                <div class="flex items-center gap-4 z-10">
                    <div
                        class="w-12 h-12 bg-[#fffcdc] rounded-2xl flex items-center justify-center text-[#14281d] rotate-3 shadow-lg"
                    >
                        <Globe size={28} strokeWidth={2.5} />
                    </div>
                    <div>
                        <h2
                            class="text-[#fffcdc] font-black text-2xl tracking-tighter leading-none"
                        >
                            MONITOR
                        </h2>
                        <p
                            class="text-[#fffcdc]/40 text-[10px] uppercase tracking-[0.3em] font-bold"
                        >
                            Elecciones Subnacionales
                        </p>
                    </div>
                </div>

                <div class="relative z-10 my-12">
                    <span
                        class="text-[#fffcdc]/30 uppercase tracking-[0.5em] text-xs font-bold mb-4 block"
                        >Onboarding 01</span
                    >
                    <h1
                        class="text-6xl md:text-8xl font-black text-[#fffcdc] leading-[0.85] tracking-tighter mb-6"
                    >
                        Poder <br />
                        <span
                            class="text-transparent bg-clip-text bg-gradient-to-r from-[#fffcdc] to-[#fffcdc]/20"
                            >Ciudadano.</span
                        >
                    </h1>
                    <p
                        class="text-[#fffcdc]/60 text-lg md:text-xl max-w-md font-medium leading-relaxed"
                    >
                        La plataforma definitiva para auditar, conocer y elegir
                        a tus representantes en 2026.
                    </p>
                </div>

                <div class="grid grid-cols-2 gap-6 z-10">
                    <div class="flex items-center gap-3 text-[#fffcdc]/80">
                        <div
                            class="p-2 bg-white/5 rounded-lg border border-white/5"
                        >
                            <ShieldCheck size={20} />
                        </div>
                        <span class="text-sm font-semibold"
                            >Datos Verificados</span
                        >
                    </div>
                    <div class="flex items-center gap-3 text-[#fffcdc]/80">
                        <div
                            class="p-2 bg-white/5 rounded-lg border border-white/5"
                        >
                            <Zap size={20} />
                        </div>
                        <span class="text-sm font-semibold"
                            >Actualización Real-time</span
                        >
                    </div>
                </div>

                <div
                    class="absolute -right-20 top-1/2 -translate-y-1/2 opacity-10 pointer-events-none z-0 animate-float"
                >
                    <Fingerprint size={460} strokeWidth={0.5} color="#fffcdc" />
                </div>
            </div>

            <div
                class="lg:col-span-5 bg-[#fffcdc] p-8 md:p-16 flex flex-col justify-center relative"
            >
                <div
                    class="absolute top-12 left-8 right-8 flex justify-between items-center"
                >
                    <div class="flex gap-1.5">
                        <div class="w-8 h-1.5 bg-[#14281d] rounded-full"></div>
                        <div class="w-2 h-1.5 bg-[#14281d]/10 rounded-full">
                        </div>
                        <div class="w-2 h-1.5 bg-[#14281d]/10 rounded-full">
                        </div>
                        <div class="w-2 h-1.5 bg-[#14281d]/10 rounded-full">
                        </div>
                    </div>
                    <span
                        class="text-[10px] font-black text-[#14281d]/40 uppercase tracking-widest"
                        >Paso 1 de 4</span
                    >
                </div>

                <div class="space-y-8">
                    <div class="space-y-4">
                        <h3
                            class="text-4xl font-extrabold text-[#14281d] tracking-tight leading-tight"
                        >
                            ¿Listo para tomar el control?
                        </h3>
                        <p class="text-[#14281d]/70 font-medium">
                            Antes de comenzar, personalizaremos tu experiencia
                            según tu región.
                        </p>
                    </div>

                    <div class="space-y-4">
                        <button
                            class="w-full group bg-[#14281d] text-[#fffcdc] p-5 rounded-2xl font-bold text-lg flex items-center justify-between hover:shadow-2xl hover:shadow-[#14281d]/30 transition-all hover:scale-[1.02] active:scale-[0.98]"
                        >
                            <span>Empezar registro</span>
                            <ArrowRight
                                className="group-hover:translate-x-1 transition-transform"
                            />
                        </button>

                        <button
                            class="w-full bg-[#14281d]/5 text-[#14281d] p-5 rounded-2xl font-bold text-lg hover:bg-[#14281d]/10 transition-colors"
                        >
                            Explorar como invitado
                        </button>
                    </div>

                    <p
                        class="text-center text-[11px] text-[#14281d]/40 font-bold uppercase tracking-tighter"
                    >
                        Al continuar, aceptas nuestros términos de
                        transparencia.
                    </p>
                </div>

                <div class="absolute bottom-8 left-0 right-0 text-center">
                    <span
                        class="text-[9px] font-black text-[#14281d]/20 uppercase tracking-[0.6em]"
                        >Plataforma Cívica 2026</span
                    >
                </div>
            </div>
        </div>
    </main>
</Layout>

<style>
    :global(body) {
        background-color: #0d1611;
        margin: 0;
    }

    h1,
    h2,
    h3 {
        text-wrap: balance;
    }

    /* Animación General de Entrada */
    main {
        animation: fadeIn 0.8s ease-out forwards;
    }

    @keyframes fadeIn {
        from {
            opacity: 0;
            transform: translateY(10px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    /* ANIMACIÓN DE FLOTACIÓN */
    .animate-float {
        animation: floating 8s ease-in-out infinite;
    }

    @keyframes floating {
        0%,
        100% {
            transform: translateY(-50%) translateX(0px) rotate(0deg);
        }
        33% {
            transform: translateY(-53%) translateX(10px) rotate(2deg);
        }
        66% {
            transform: translateY(-47%) translateX(-5px) rotate(-1deg);
        }
    }
</style>
